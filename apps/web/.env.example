# See the root .env.example at the repository root for the full list of
# environment variables used by ChessTalk. Copy that file to .env.local
# here (or set variables via your shell) before running `bun dev:web`.
#
# Required for this app:
#   NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
#   CLERK_SECRET_KEY
